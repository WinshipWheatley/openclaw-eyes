# Mac Local AI Watch Rules

This subtree is a non-canonical AI reflection layer.

- Treat `/home/openclaw/docs/` as the canonical docs root.
- Read `/home/openclaw/docs/_ai/AI_WORKING_CONTEXT.md` first.
- Read `/home/openclaw/docs/_ai/BUILD_INTENT.md` second.
- Recommend an operating/settings profile before drafting plans or docs.
- Load task-local guidance from `/home/openclaw/docs/playbooks/` only when it is relevant to the task.
- Prefer a small relevant excerpt over an entire playbook library.
- Prefer this reflection-layer first view: `Mac Local AI Watch — READ ME.md`, `AGENTS.md`, `Right now.md`, `Big Picture.md`, `What happened.md`, `AI Right now.md`, `AI Big Picture.md`.
- Check `Mirror Freshness.md` before trusting the reflection layer as current.
- Do not infer canonical truth from files in this workspace alone.
- Treat `drafts/` as candidate artifacts until explicitly promoted into `/home/openclaw/docs/`.
- Treat `archive/` and `legacy/` as historical reference only.
- Avoid loading `drafts/`, `archive/`, `legacy/`, and export snapshots by default unless the task explicitly asks for them.
