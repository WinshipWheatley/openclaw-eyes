# Operator Harness Visual Import Freshness Bridge

Status: docs-only source-set freshness bridge. This file is not a source-set folder, not a Launch Packet, not implementation authority, and not a Mac sync/import instruction.

Generated/reviewed: 2026-05-05

## Purpose

Orient the next ChatGPT Project source-set decision after the docs-only Operator Harness visual/spatial packet import.

This bridge exists because the current 24-file ChatGPT Project source set is still useful, but it was generated before the canonical repo gained the visual/spatial packet and two Operator Harness doctrine/research docs.

## Freshness Finding

Prior uploaded source-set baseline:

- 24 upload files total.
- Generated from source commit `c5947e7fbdf8952824ddb60fb8b8203bdea28e95`.
- Stale by its own `MANIFEST.md` rules relative to latest pushed import commit `bb635e66b9142acb1e3f55be5064e6bd8702622c` (`bb635e6 docs(app): import mac operator harness visual packet`).

Freshness result: the prior 24-file Project source set should not be treated as current manifest authority for work that depends on the newly imported visual/spatial packet, Operator Harness doctrine/research docs, or the import manifest.

## Prior Source Set Still Matters

Keep using the prior source set as valuable background for:

- Launch Ladder model, Launch Packets, Approval Receipts, and sequence posture.
- Evidence/freshness rules, stale checks, proof expectations, and manifest discipline.
- Authority boundaries, withheld surfaces, and human control requirements.
- Routing/workspace posture and ChatGPT Project upload expectations.
- Fixture-state meanings and read-only Mission Control display posture.
- Product taste, anti-slop checks, and operator experience evaluation.
- Knowledge Substrate doctrine: compile-first, not vanilla RAG; raw files as evidence, parsed/extracted content as evidence, compiled notes as interpretation, and operator promotions as explicit decisions.

Do not discard the prior set just because it is stale. Treat it as the baseline context plus this bridge's addendum until a deliberate full refresh replaces it.

## Newly Canonical Planning Context

The next ChatGPT Project planning context should explicitly add these canonical repo materials:

- `docs/planning/launch_ladder/visual/operator_harness_north_star_v1/`
- `docs/planning/launch_ladder/operator_harness_research/DOMAIN_AGNOSTIC_OPERATOR_SYSTEMS.md`
- `docs/planning/launch_ladder/operator_harness_research/STUDIO_BORN_OPERATOR_INTELLIGENCE.md`
- `docs/planning/launch_ladder/mac_import_planning/import_manifests/operator_harness_docs_only_import_20260504_235300.md`

Use the canonical PC/WSL repo paths above as the source of record. Mac `OpenClaw_Watch` remains reflection/source-reference only.

## Upload Recommendation

Recommendation: use a small delta/addendum alongside the existing 24-file source set for the next narrow Operator Harness planning pass.

Reason:

- The prior 24-file source set remains structurally useful for Launch Ladder, evidence/freshness, authority, routing, fixture, taste, and Knowledge Substrate doctrine.
- The import is docs-only and can be carried as a compact addendum without changing implementation authority, withheld surfaces, or the immediate planning lane.
- A bridge keeps the next chat focused on whether the visual/spatial packet and doctrine docs change build-readiness planning before spending a full 24-file refresh.

Important limit: bridge-only upload is an addendum, not a fresh manifest baseline. If the next ChatGPT Project session needs current manifest authority at `bb635e6`, changes source-set membership, changes folder purpose, changes withheld surfaces, cannot keep baseline/addendum separation clear, or moves to a new source-set folder, regenerate the full 24-file source set instead.

## Next Planning Recommendation

Continue Operator Harness build-readiness planning before moving to the next source-set folder.

The next useful planning step is to absorb the visual/spatial packet and the two doctrine/research docs into the readiness posture: what they change about Mission Control taste, operator-world model, signal-flow vocabulary, evidence display, and domain-agnostic product framing.

Do not move to the next source-set folder merely because the prior set is stale. Move only after the current planning pass has extracted the new visual/spatial and doctrine implications and can name why the next folder needs different membership or purpose.

## Hard Boundaries

This bridge does not authorize:

- app implementation;
- backend/schema/SQLite implementation;
- ingestion;
- Mac file import;
- sensitive/private inspection;
- file cleanup, move, delete, or sync;
- treating Mac `OpenClaw_Watch` as canonical.

PC WSL `/home/openclaw` is canonical. Mac `OpenClaw_Watch` is reflection/source-reference only.
