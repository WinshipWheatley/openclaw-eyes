# MCP Progressive Discovery Profiles

Status: implemented hardened default for `.mcp.json`; unlock profiles remain explicit gated contracts.

Authority: this document records the active hardened MCP default and the bounded profile contracts required for any future unlock. It does not authorize service, scheduler, provider, Gmail, Telegram, Hermes runtime, live model, or messaging expansion.

Purpose: MCP access should start with the smallest deterministic packet and expand only after an explicit unlock trigger, a gate, and a logged reveal artifact. Broad filesystem or tool disclosure is trusted-local development behavior, not agent default behavior.

## Profile Contract

| Profile | Allowed roots/tools | Withheld surfaces | Unlock trigger | Approval/policy/verifier gate | Reveal/unlock artifact |
| --- | --- | --- | --- | --- | --- |
| `default-docs-read` | Read-only filesystem access to non-sensitive control/docs roots only: `docs/operations` and `docs/specs`. Root-level control files such as `OPENCLAW_RUNTIME.md` and `USER.md` should be supplied by deterministic packet or a separate exact-file mechanism, not by exposing the whole repo root. Tools should be read/search only. | Repo-wide source, `/mnt/c/OpenClawShared/openclaw-vault`, `/mnt/c/OpenClaw/logs`, `/mnt/c/OpenClawLegalPrivate`, Hermes home/session/state paths, Gmail/Drive data, messaging tools, terminal/process tools, write tools, provider tools, plugin discovery, and broad skill management. | The operator or a deterministic first packet identifies a specific lane need that cannot be answered from the default docs packet. | Static root allowlist, no write/send/terminal/process/provider/plugin tools, and a focused `.mcp.json` profile test. | Default MCP profile manifest listing allowed docs, withheld roots/tools, and denied unlocks. |
| `repo-source-read-unlocked` | Read-only repo source access for named files or a narrow source subtree under `/home/openclaw`, with search limited to the approved lane. | Ignored/runtime/private paths, secrets, env files, `.google-secrets`, SSH/token paths, Hermes home/session/state paths, Legal private data, shared vaults, logs, messaging sends, writes, terminal/process tools, provider calls, and external MCP/plugin discovery. | A reviewed first packet names the source lane, target module(s), and why docs-only context is insufficient. | Lane-scoped source allowlist, denylist for private/runtime paths, git status baseline, and no secret/env inspection. | Source-read unlock record with exact roots/files, reason, requester, and expiry or end condition. |
| `runtime-artifact-read-unlocked` | Read-only access to exact artifact files or manifests named by a lane, such as bounded JSON/Markdown evidence artifacts. No whole log-root access. | `/mnt/c/OpenClaw/logs` as a root, raw private logs, broad runtime state, service controls, secrets, Legal matter data, private correspondence, Hermes session/state DBs, and write/send/terminal/provider tools. | A deterministic packet cites a specific artifact path or manifest required for verification. | Exact-path allowlist, artifact-type validation where available, redaction policy, and no directory-wide log browsing by default. | Artifact reveal note naming exact paths, source lane, redaction status, and consuming task. |
| `shared-vault-operator-approved` | Read-only access to operator-approved exact paths inside `/mnt/c/OpenClawShared/openclaw-vault`; never the whole vault by default. | Entire shared vault root, unrelated operator material, Legal private data, raw private logs, secrets, credentials, messaging sends, writes, terminal/process tools, provider calls, and external MCP/plugin discovery. | Explicit operator approval names the needed vault item and the task it supports. | Chief/Guardian or equivalent operator approval, exact-path allowlist, redaction review, and time-limited unlock. | Vault unlock approval record with approved paths, withheld siblings, purpose, and closure note. |
| `hermes-gateway-advisory` | OpenClaw Hermes gateway mode only, using read-heavy advisory tools such as `read_file`, `search_files`, `vision_analyze`, `skills_list`, `skill_view`, `todo`, and `clarify`. | Normal Hermes CLI/messaging/API toolsets, MCP/plugin discovery, external provider fallback, messaging sends, terminal/process tools, write/patch tools, memory mutation, cronjob tools, Home Assistant tools, Hermes session/state DBs, shared vaults, and logs. | Explicit request for Hermes advisory synthesis through OpenClaw gateway mode or a bounded manual proof workflow. | `HERMES_OPENCLAW_MODE=gateway`, gateway toolset resolution, external fallback disabled, gateway policy tests, file-safety deny rules, and advisory/non-canonical boundary. | Gateway toolset manifest, advisory proof artifact, denied-surface reason, or proof-check result. |
| `trusted-local-dev` | Operator-controlled local development profile. May include broader repo access and normal local development tools when the operator intentionally selects it. | Legal private data, secrets/env/credential inspection, shared vaults, raw logs, Hermes session/state DBs, external sends, live provider calls, and destructive/service actions unless separately approved. | Operator explicitly chooses trusted local development for a bounded repo task. This must not be the agent default. | Git status baseline, runtime-law approval rules for high-risk actions, no secret/env inspection by default, and normal test/diff verification. | Dev-session status record, changed-files list, checks run, and approval receipts for any high-risk unlocks. |

## Default Hardening Rule

The hardened default profile must not expose broad filesystem roots, vaults, logs, Hermes runtime state, messaging tools, terminal/process tools, write tools, provider tools, or MCP/plugin discovery. If a lane needs one of those surfaces, it must move through an explicit unlock profile with a gate and a reveal artifact.

## Current Implementation Note

Current `.mcp.json` uses the `default-docs-read` filesystem profile and defaults filesystem MCP access to `docs/operations` and `docs/specs` only. Repo source, vaults, logs, Legal private paths, Hermes runtime paths, provider tools, messaging tools, terminal/process tools, write tools, and MCP/plugin discovery remain withheld unless an explicit unlock profile or trusted local development profile is selected with its required gate and reveal artifact.
