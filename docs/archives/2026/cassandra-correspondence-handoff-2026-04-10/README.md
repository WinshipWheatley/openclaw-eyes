# Cassandra correspondence handoff

These files were created in the **Mac Local AI Watch workspace** and are packaged here for **manual transfer** into the real repo workflow.
They are **not yet real-repo implementation**.

## Files
- `cassandra-pilot-playbook.md` — policy/playbook artifact describing the approval-gated pilot shape, guardrails, rollout, and operating model.
- `cassandra-operational-spec.md` — policy/spec artifact defining the system prompt, input/output contract, review queue fields, audit shape, and implementation defaults.

## Artifact types
- **Policy/spec artifacts:** `cassandra-pilot-playbook.md`, `cassandra-operational-spec.md`
- **Candidate implementation artifacts:** none in this watch-workspace handoff folder

## Note
A related TypeScript/Zod contract module was created separately in a live repo environment, not in this Mac watch workspace, so it is not included here as a watch-workspace artifact.
