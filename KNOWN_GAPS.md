# KNOWN_GAPS.md
_Generated: 2026-03-17 | Based on actual repo audit — reflects real state, not aspirational._

---

## 1. Album Data — Only 3 of 12 Songs Have CSV Entries

**What exists:** `album_work_log.csv` has rows for: 1 In A Million, The Future, Blue Weather
**What's missing:** 9 songs from `_ALBUM_SONGS` have no CSV row at all
**Impact:** Batch planner, batch overview, mix briefs all operate on 3 songs only. Cross-album recommendations are incomplete.
**Why:** Album sessions have only been run for 3 songs. Each song row is created by running an album session to completion.
**Fix path:** Run `album` session for each of the 9 missing songs, or seed rows manually.

---

## 2. Session Stuck — Blue Weather, Phase `follow_up`, Turn 0

**State:** `chief_session.json` shows `active_workflow=album, status=active, phase=follow_up, song=Blue Weather, turn=0`
**Impact:** Every new message may be intercepted as an album session continuation until user says "cancel" or "restart"
**Why:** Session was started but not completed. Turn 0 in follow_up means the first follow-up question was asked but no answer was received.
**Fix path:** Send "album status" to see where it left off, then continue or "cancel album".

---

## 3. Worker Stdout Logs All 0 Bytes — CLOSED

**What exists:** `/mnt/c/OpenClaw/logs/listener.out`, `worker.out`, `memory_worker.out`, `state_worker.out` — all 0 bytes
**Exception:** `billing_brain.out` is 6.2KB — the only non-empty out file
**Impact:** No visibility into worker errors or activity without adding explicit logging
**Why:** Workers do not emit stdout. Python `print()` calls may be buffered and never flushed when run under nohup with redirected output.
**Fix path:** Add `flush=True` to print calls, or redirect stderr explicitly, or add file-based logging.

---

## 4. Google Calendar — No Credentials Configured

**File:** `chief_calendar_brain.py`, `google_access_broker.py`
**State:** Dry-run mode only. No credentials exist at either path (legacy or broker).
**Blocker:** External manual step required before any code work can continue. Credentials must come from Google Cloud Console — they cannot be generated by Claude Code.
**Impact:** "what's my week" / "calendar" queries return dry-run placeholder, not real calendar data

**Fix path (manual — outside Claude Code):**
1. console.cloud.google.com → create project → enable Google Calendar API
2. APIs & Services → Credentials → Create OAuth 2.0 Client ID → Desktop app → Download JSON
3. `mkdir -p /home/openclaw/.google-secrets && chmod 700 /home/openclaw/.google-secrets`
4. Copy downloaded file to `/home/openclaw/.google-secrets/credentials.json`
5. `chmod 600 /home/openclaw/.google-secrets/credentials.json`
6. `source ~/chief_env/bin/activate && python3 /home/openclaw/google_access_broker.py --auth`
7. `chmod 600 /home/openclaw/.google-secrets/token.json`

**After manual steps:** Return to Claude Code — Phase 3 live wiring (uncomment executor, refactor _fetch_events, live test, retire legacy auth) must run in a Claude Code session.

---

## 5. Gmail / Twilio — Optional Credentials Not Configured

**Files:** `chief_email_brain.py`, `chief_sms_brain.py`
**State:** `GMAIL_USER`, `GMAIL_APP_PASSWORD`, `TWILIO_*` env vars not present in `.chief.env` (confirmed by env dependency table)
**Impact:** Email/SMS brains operate in draft-only mode. Drafts are logged but not sent.
**Fix path:** Add credentials to `/home/openclaw/.chief.env` when live sending is needed.

---

## 6. Website — No Live Site to QA

**File:** `chief_website_qa.py`
**State:** Domain `deeppocketrecords.com` registered March 15, 2026. No site deployed yet.
**Impact:** `website qa` command will get connection refused or empty page — crawler has nothing to crawl
**Note:** `requests` and `beautifulsoup4` may not be installed in `chief_env`
**Fix path:** Deploy a holding page or Fundo teaser. Then QA becomes useful.

---

## 7. `chief_reply_worker.py` — Possible Double-Reply Risk — CLOSED

**Closed:** 2026-03-22. chief_reply_worker.py confirmed never launched by start_chief.sh, no callers found, and removed from the repo.

---

## 8. DEEPPOCKET.md — Mostly Template Stubs

**File:** `/home/openclaw/DEEPPOCKET.md`
**State:** Roster, Releases, Publishing sections are all blank template stubs
**Impact:** Label reference that brains may consult is incomplete
**Fix path:** Fill in as releases and artist details solidify.

---

## 9. `chief_album_brain_legacy.py` — Dead Code — CLOSED

**Closed:** 2026-03-22. chief_album_brain_legacy.py confirmed no callers and removed from the repo.

---

## 10. Legacy Polling Brains — Not Started by Default

**Files:** `chief_album_brain.py`, `chief_billing_brain.py` (legacy polling mode)
**Started by:** `start_openclaw_brains.sh` — a separate script not called by `start_chief.sh`
**State:** Both have standalone polling loops; `chief_album_brain.py` is also used as a library by the main stack
**Impact:** If `start_openclaw_brains.sh` is not run separately, the polling loops are silent. Main stack still works via direct `handle()` calls.
**Fix path:** Document clearly: polling mode is optional/legacy. Main stack does not need it.

---

## 11. `chief_watcher_brain.py` — Systemd Unit Not Confirmed — CLOSED

**Closed:** 2026-03-22. Systemd unit created: `/etc/systemd/system/openclaw.service`. Type=oneshot/RemainAfterExit. Enabled and verified active. Auto-starts on WSL boot. All 9 daemons confirmed running post-setup.

---

## 12. `chief_obsidian_sync.py` — Not in Start Scripts

**File:** `chief_obsidian_sync.py`
**State:** Exists and is tracked. Not called in `start_chief.sh` or `start_openclaw_brains.sh`.
**Impact:** CSV → vault YAML sync is not automatic. Must be triggered manually or via Telegram intent.
**Fix path:** Add sync trigger to album session finalize, or add to a periodic cron.

---

## 13. `brainstorm_log.json` — No Cleanup / Archiving

**File:** `/mnt/c/OpenClawShared/album/brainstorm_log.json`
**State:** ~25 items. Grows unbounded. No archiving or pruning logic.
**Impact:** Minor now. Will become slow/messy at scale.
**Fix path:** Add an archive command that moves `status=archived` items to a separate file.

---

## 14. `chief_approval_bridge.py` — No Timeout Enforcement — CLOSED

**State:** `choice_pending.json` records a `requested_at` timestamp. Timeout logic exists in `chief_approval_brain.py` (blocking gate) but bridge timeout is not enforced at poll time.
**Impact:** A stale bridge request could block indefinitely if user never responds
**Fix path:** Check `requested_at` on read; expire after N minutes and log the auto-deny.

---

## 15. No Smoke Test Suite — CLOSED

**Closed:** 2026-03-22. `test_smoke.py` created at `/home/openclaw/test_smoke.py`. 26 tests across 5 groups: module imports, intent detectors, capability flags, stack process health, log file health. 26/26 passing in 0.099s. No Ollama, Telegram, or credentials required.
